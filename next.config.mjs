/** @type {import('next').NextConfig} */
const nextConfig = {};
export default nextConfig;

// OpenNext Cloudflare dev helper - commented out because @opennextjs/cloudflare is not installed.
// To use the modern OpenNext adapter, install the package and uncomment this line.
// import('@opennextjs/cloudflare').then(m => m.initOpenNextCloudflareForDev());
